---
- name: python batch job automation
  hosts: localhost
  gather_facts: false

  tasks:
    - name: Install specified python requirements
      ansible.builtin.pip:
        name:
          - requests
